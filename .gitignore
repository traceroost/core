.DS_Store
out
dist
node_modules
.vscode-test/
*.vsix
standalone/server.js
standalone/server.js.map
standalone/cli.js
standalone/cli.js.map
media/dashboard.js
media/dashboard.js.map
media/dashboard.css
media/dashboard.css.map
media/sidebar.js
media/sidebar.js.map
# Captured fixture files contain real telemetry — opt-in to commit specific ones
demo/fixtures/*.json
# Session data exports
export_*.json
# Local staging
.staged-issues/
.staged-features/
# Scratch research notes — not project docs
.research/
# Claude Code per-developer settings (permissions, hooks — not project config)
.claude/settings.json
.claude/settings.local.json
# Local pnpm package store, when created inside the repo instead of pnpm's global location
.pnpm-store/
# Local environment overrides for `npm run local` (e.g. TRACEROOST_ORG_ENV) — never committed
.env
# Browser UX artifacts
test-results/
playwright-report/
